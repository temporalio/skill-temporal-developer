# Activity Describe Payloads (TypeScript)

TypeScript docs show Workflow handle describe for observability. For Activities, use CLI describe for Standalone Activities.

Opt-in payload inclusion:
- Include input, result, heartbeat details, and last failure payloads only when available and required.
